# (Python 3.9 and older)
# Shifted floor to 1.21.0 to satisfy fast-hdbscan
numpy>=1.21.0,<2.0.0 ; python_version < "3.10"
scikit-learn>=1.0.0,<1.1.0 ; python_version < "3.10"

# Modern environment flexibility (Python 3.10 and newer)
# Capped below 2.0.0 to prevent C-API compilation breaks with Numba
numpy>=1.21.0,<2.0.0 ; python_version >= "3.10"
scikit-learn>=1.0.0 ; python_version >= "3.10"

# Universal packages
fast-hdbscan
numba
matplotlib
psutil